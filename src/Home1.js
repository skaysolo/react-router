import Typography from '@mui/material/Typography';


export default function Home() {
    return (
        <Typography variant='button' fontSize={30} margin={85}>Vítejte v mém react projektu !</Typography>
    )
};